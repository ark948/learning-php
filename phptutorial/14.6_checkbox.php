<?php


// A checkbox allows you to select a single value for submission in a form.

// example builds up the previous example
// refer to form_processing folder

// also we can use isset to check if a checkbox is selected
isset($_POST['checkbox_name']); 
// if checkbox is not selected, this will return false