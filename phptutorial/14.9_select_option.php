<?php


// The <select> is an HTML element that provides a list of options. 

// important attributes are: id, and name

// select element can have multiple option elements inside it
// each option has a value

// If an option doesn’t have the value attribute, the value attribute defaults to the text inside the <option> element.

// you can add 'selected' attribute to one option, which will be selected when page loads first

// example of this lesson builds up on previous -> form_processing2 (added select element)

// to enable multiple selection, add the 'multiple' attribute to <select>
// <select name="colors[]" id="colors" multiple>

// example -> form_processing3